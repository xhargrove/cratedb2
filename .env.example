# Copy to `.env` / `.env.local` for local development.

# PostgreSQL connection string (required at runtime for DB access)
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/cratedb?schema=public"

# Optional: pino log level (fatal | error | warn | info | debug | trace | silent)
# LOG_LEVEL=info
